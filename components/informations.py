from components.component import Component

class Information(Component):

    def __init__(self, name, title, contact, sites): #name, title, contact, sites
        super().__init__(name, title, contact, sites)